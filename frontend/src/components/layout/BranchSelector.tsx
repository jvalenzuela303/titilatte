import React from 'react'

/**
 * Selector de sucursal deshabilitado — el sistema opera con una única tienda física.
 * Componente preservado para referencia futura.
 */
const BranchSelector: React.FC = () => null

export default BranchSelector
